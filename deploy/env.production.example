# Copy to .env at the repository root. Replace blanks with values in the host's
# secret manager or in this untracked local environment file.
DOMAIN=
FRONTEND_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
POSTGRES_USER=initprep_app
POSTGRES_PASSWORD=
JWT_SECRET=
JWT_EXPIRATION=86400000
JPA_SHOW_SQL=false
OPENAI_API_KEY=
JUDGE0_URL=https://ce.judge0.com
JUDGE0_API_KEY=
