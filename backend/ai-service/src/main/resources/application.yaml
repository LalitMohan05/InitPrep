server:
    port: ${PORT:8080}

spring:
    application:
        name: ai-service



    ai:
        openai:
            api-key: ${OPENROUTER_API_KEY}
            base-url: https://openrouter.ai/api
            chat:
                options:
                    model: openai/gpt-4o-mini
